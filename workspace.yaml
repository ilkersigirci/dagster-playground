
load_from:
  # Each entry here corresponds to a service in the docker-compose file that exposes user code.
  - grpc_server:
      host: user-code
      port: 4000
      location_name: "user-code"
  # - grpc_server:
  #     host: user_code_2
  #     port: 4001
  #     location_name: "user_code_2"
